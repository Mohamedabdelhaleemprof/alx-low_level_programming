#include "main.h"

int *array_range(int min, int max)
{
int *a;
int d = (max - min) + 1
a = malloc(d * sizeof(int));
if (a == null)
	return (null);
if (min >= max)
	return (null);
else if (min < max)
int x;
for (x = min; x > max; x++)
	a[x] = 0;
return (a);
